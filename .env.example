# Postgres connection string. For local dev, use Postgres.app or Docker.
# Supabase free tier also works — copy from Project Settings > Database.
DATABASE_URL="postgresql://user:password@localhost:5432/contextpilot"

# Anthropic API key. Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."
